port: 7880
rtc:
  tcp_port: 7881
  port_range_start: 50000
  port_range_end: 60000
  use_external_ip: true

keys:
  # key: secret pairs
  # You can also use env var LIVEKIT_KEYS
  "devkey": "secret"

logging:
  level: info
